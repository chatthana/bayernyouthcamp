$(document).ready(function() {

  // window.sr = ScrollReveal();
  // sr.reveal('header nav ul > li', {duration:1000}, 100);
  // sr.reveal('.sponsor-img', {duratiob:1000}, 100);
  // sr.reveal('.reveal');

  // $('.datepicker').datepicker();

  $('.flexslider').flexslider({
    animation:'slide',
    directionNav:true
    // controlNav:'thumbnails'
  });

});
